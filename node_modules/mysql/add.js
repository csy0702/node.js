
var mysql=require("mysql");
var connenction=mysql.createConnection({
    host:"127.0.0.1",
    port:3306,
    database:'stusys',
    user:"root",
    password:"279293"
});



connenction.connect(function (err) {
    if(err){
        console.info("与数据库的连接失败");
    }else {
        connenction.query("insert into classInfo set ?",{cid:0,cname:"yc27",status:1}, function (err,result) {
            if(err){
                console.info("班级信息添加失败..");
            }else {
                console.info("班级信息添加成功..");
                console.info(result);
                console.info(result.insertId);//获取刚才添加的那条数据的编号
            }
            connenction.end();//关闭连接

        })
    }
})
