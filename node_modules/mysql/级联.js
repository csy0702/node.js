/**
 * Created by Administrator on 2016/9/7.
 */
var connection=require("MySQL").conn;
connection.connect(function(err){
    if(err){
        console.info("连接数据库失败...");
    }else {
        //如果两个表中有重复的名称，那么就会覆盖
        //解决方案一  将同名列重命名
       /* connection.query("select s.*,c.cid classId,cname,status from stuInfo s,classInfo c where s.cid=c.cid", function (err,result) {
            if(err){
                console.info("查询学生信息失败.."+err);
            }else {
                console.info(result);
            }
            connection.end();
        });
        //解决方案二   将每个表中的数据按表名或别名分开显示

        connection.query({sql:"select * stuInfo s,classInfo c where s.cid=c.cid",nestTables:true}, function (err,result) {
            if(err){
                console.info("查询学生信息失败.."+err);
            }else {
                console.info(result);
            }
            connection.end();
        });
        //解决方案三   在每个字段前面加上所属表的名称或别名
*/
        connection.query({sql:"select * stuInfo s,classInfo c where s.cid=c.cid",nestTables:"_"}, function (err,result) {
            if(err){
                console.info("查询学生信息失败.."+err);
            }else {
                console.info(result);
            }
            connection.end();
        });
    }
})